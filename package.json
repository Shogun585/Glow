{
  "name": "workspace",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck": "tsc --build"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "prettier": "^3.9.5"
  }
}